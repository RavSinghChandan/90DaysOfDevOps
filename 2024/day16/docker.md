# Doker run - To see that container is running
# Docker stats - What is the statistics data for the docker contianer
# Docker up - What are the processes running inside the docker container
# Docker inspect - What are the process running inside the doker continer or image
# Docekr port - On which port the continer is running
# Docker save - To save the image into a t *tar* archive 
# Docker load - To loed the image from the *tar* arhive 